import {filter, uniq} from 'loadash'

const InstallationService = {

    /* transform json object into data that can be consumed by highcharts*/
    transformData: function(jsonInput) {


        //inspect the value
    },

};

export default InstallationService;