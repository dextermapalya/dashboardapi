import Login from 'components/Login';
import Home from "components/home";

export default {
    Home: {
        component: Home,
        path: '/'
    },
    Login: {
        component: Login,
        path: '/login'
    },
};